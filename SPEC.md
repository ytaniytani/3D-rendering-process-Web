# SPEC.md: 3DCG描画のしくみ 超丁寧・複数ページ版 開発仕様書（v2）

## 0. v1からの変更点（このリライトの目的）

v1は全7章を1ページに詰め込んだため、1つ1つの概念の説明が数行で終わってしまい、
初学者には「単語は出てきたが、なぜそうなるのか分からない」ページになっていた。

v2では以下の方針に全面的に切り替える。

1. **1ページ=1テーマの複数ページ構成にする。** 総ページ数が多くなることは問題にしない。
   1ページあたりの情報量を絞り、そのぶん1つの概念を何段落でも使って説明する。
2. **すべての概念を「説明テンプレート」に沿って書く。** どの概念も必ず
   「①なぜこの話をするのか → ②身近な例え → ③仕組みの説明（段階分割） → ④触って確認（インタラクティブ） → ⑤よくある誤解・つまずき → ⑥このページのまとめ」
   の6ブロックで構成する。読者は各ページで同じリズムで学べる。
3. **専門用語は初出時に必ず「ふりがな＋一言定義＋詳細説明」をセットにする。**
   用語だけ出して先に進むことを禁止する。ページ下部に用語集も置く。
4. **1つのインタラクティブ機能に対して、操作手順を必ず文章で誘導する。**
   「スライダーを動かしてみましょう」ではなく「まずFOVスライダーを50°から100°までゆっくり上げてください。
   このとき右画面のオレンジの枠が横に開くのを見てください。次に…」のように、
   何を見るべきかを1ステップずつ指示する（ガイド付きツアー）。
5. **各ページ末尾に理解度チェック（3問程度の選択式クイズ）を置く。**
   その場で正誤とやさしい解説を表示する。

対象読者・数式排除・Three.jsによるエミュレーション方針はv1と同じ。

---

## 1. 対象読者と前提（v1から継承）

* 文系のゲームクリエイター、アーティスト、3D環境・背景モデラー、ゲームプランナー。
* 中学数学（正負の数、2D座標平面、比例）まで。行列・三角関数・ベクトルの知識は前提にしない。
* 「コードや数式を見ると脳が拒絶するが、因果関係（なぜ重い？なぜ速い？）はロジックで正しく知りたい」層。
* 数式・コードは本文に一切出さない。行列は「変換のレシピカード」のような比喩で扱う。

---

## 2. サイト全体構成（複数ページ）

静的サイト。各ページは共通ヘッダー（章ナビ）＋共通フッター（前へ/次へリンク）を持つ。

```
index.html                     … 表紙・学習マップ（全章の地図と読む順序）
pages/
  ch1-coordinates.html         … 第1章: 座標とは何か（X,Yの復習から奥行きZへ）
  ch1b-vertex-polygon.html     … 第1章b: 頂点とポリゴン（点を結ぶと面になる）
  ch2a-local-world.html        … 第2章a: ローカル空間とワールド空間
  ch2b-view.html               … 第2章b: ビュー空間（カメラが世界の中心になる）
  ch2c-projection.html         … 第2章c: 投影とクリップ（映る範囲を決める）
  ch2d-rasterize.html          … 第2章d: ラスタライズ（三角形がピクセルになる）
  ch3a-gbuffer.html            … 第3章a: G-Buffer（絵の「素材」を先に作る）
  ch3b-lighting.html           … 第3章b: ライティングパス（素材に光を当てる）
  ch3c-transparent-post.html   … 第3章c: 半透明とポストプロセス（仕上げ）
  ch4-quad-overdraw.html       … 第4章: クアッドの罠（なぜ遠景の高精細モデルは重い？）
  ch5-meshlet.html             … 第5章: Meshlet（塊で仕分けるハードウェアの新常識）
  ch6-nanite.html              … 第6章: Nanite（エンジンによる全自動化と裏ルート）
  ch7-summary.html             … 第7章: まとめとアーティストの未来
glossary.html                  … 全用語集（五十音順、各ページからリンク）
```

読了目安は1ページ10〜15分。表紙の学習マップに「全部で13ページ、順番に読めば必ず繋がる」ことを明示する。

### 2.1 共通ページ構造

すべての章ページは次のセクション順で構成する。

1. **このページで分かること**（箇条書き3つ以内。ゴールを先に見せる）
2. **前のページの30秒復習**（前ページの要点を3行で。初学者は前提を忘れて戻ってくるため必須）
3. **本文**（説明テンプレート①〜⑥。長くなってよい。見出しを細かく切る）
4. **理解度チェック**（選択式クイズ3問、その場採点＋解説）
5. **このページの用語集**（このページで初出した用語の一覧。glossary.htmlへのリンク付き）
6. **次のページへ**（次に学ぶことの予告1〜2行＋リンク）

### 2.2 インタラクティブ機能の共通仕様

* 各デモには必ず「ガイド付きツアー」ボタンを置く。押すと吹き出しが順番に表示され、
  「今からFOVを広げます。右画面のオレンジ枠に注目」→（自動でスライダーが動く）→
  「枠が開きましたね。このとき左画面では…」のように、**デモ自身が実演**する。
  ツアー後に自由操作できる。
* 全デモに「リセット」ボタンを置く。触りすぎて分からなくなっても最初に戻れる。
* デモの直下に「ここで確認してほしいこと」チェックリスト（2〜4項目）を明記する。

---

## 3. 各ページ詳細仕様

以下、各ページの「本文の目次レベルの構成」と「デモ仕様」を定義する。
本文の各見出しには、想定する説明内容を要約して付す（実装時はこれを2〜6段落に膨らませる）。

---

### 3.1 ch1-coordinates.html「座標とは何か」

**このページで分かること:** 3Dの「座標」は中学のグラフとほぼ同じ／Zは奥行きの1本を足しただけ／マイナスの意味。

本文構成:

1. **なぜ座標の話から始めるのか** — 3DCGのすべてのデータは最終的に「数字3つの組」に行き着く。ここが曖昧だと後の全部が曖昧になる、という動機付け。
2. **中学のグラフを思い出す（X, Y）** — 横X・縦Y、原点、プラスマイナスの向き。教室の座席表・地図の「東に3、北に2」の例え。2Dキャンバスで点を打つミニデモ（後述デモA）。
3. **奥行きZを足す** — 「画面の手前と奥」という3本目の軸。例え:「教室の座席表(X,Y)に『何階か(Z)』を足すと校舎全体の住所になる」。右手系/左手系の混乱は出さず、本サイトでは「Zプラスが手前」と最初に1回だけ宣言して固定する。
4. **マイナスの座標が意味するもの** — 原点より左/下/奥。マイナス=悪いことではなく「向きが逆」なだけ。
5. **座標は「住所」であって「絵」ではない** — 点そのものには色も大きさもない。位置情報だけ。
6. **よくある誤解** — 「3Dソフトの画面に見えている物体がデータ本体だ」という誤解を正す。見えているのは住所リストを毎フレーム描き直した結果にすぎない。

デモA（2D→3D座標体験）:
* 左: X,Yの2つのスライダーと2Dグリッド。点が動く。
* 「奥行きを追加」ボタンでキャンバスがThree.jsの3Dビューにモーフィングし、Zスライダーが出現。
* ガイド付きツアー: X→Y→Zの順に自動で動かし、それぞれ「どの向きに動いたか」を吹き出しで確認させる。
* チェックリスト: 「Xを増やすと点が右に動くのを見た」「Zを増やすと点が手前に来るのを見た」等。

クイズ例: 「点(−2, 0, 3) の −2 が意味するのは？」等3問。

---

### 3.2 ch1b-vertex-polygon.html「頂点とポリゴン」

**このページで分かること:** 頂点=座標を持つ点／3つ結ぶと三角形（ポリゴン）／3Dモデル=三角形の大量の集まり。

本文構成:

1. **点に名前を付ける: 「頂点（ちょうてん／Vertex）」** — 用語導入。座標を持つ点のこと。
2. **なぜ「3点」なのか** — 2点では線にしかならない。3点あると初めて「面」が張れる。4点だと同一平面に乗らない場合がある（紙を対角にひねる例え）ので、GPUは必ず三角形で処理する。ここは初学者が最初に「なるほど」と言う定番ポイントなので厚めに書く。
3. **三角形を張る＝「点と点を結ぶ順番のリスト」を作ること** — モデルデータの正体は「頂点の住所リスト」＋「どの3点を組にするかのリスト」の2つだけ、と明言する。
4. **三角形をたくさん集めると「形」になる** — 三角形2枚で四角、12枚で立方体、数千枚で人の顔。粗いモデル/細かいモデルの違い=三角形の枚数の違い（ここが後の第4〜6章の伏線になることを予告）。
5. **よくある誤解** — 「滑らかな曲面はどこかに曲線データがある」→ ない。全部三角形。近くで見ると多面体である実例をデモで見せる。

デモB（三角形組み立てキャンバス、v1のデモを拡張）:
* 頂点3つのX,Y,Z入力欄＋3D表示（v1と同じ）に加えて:
* 「頂点を増やす」ボタンで4点目以降を追加し、三角形を2枚、3枚と張れる。
* プリセットボタン「立方体を見る」「球を見る」: 出来合いモデルをワイヤーフレーム表示し、スライダーで分割数（三角形の枚数）を変えられる。枚数カウンターを常時表示。
* ガイド付きツアーあり。

---

### 3.3 ch2a-local-world.html「ローカル空間とワールド空間」

**このページで分かること:** モデルは自分専用の原点で作られる（ローカル）／世界に配置する時に共通の原点に乗せ替える（ワールド）／Transformの正体。

本文構成:

1. **問題提起: モデラーが作った「木」はどこにある？** — DCCツールで作った時、木は「木ファイルの中の原点」を中心に作られている。ゲームの世界のどこに置くかは、まだ決まっていない。
2. **ローカル空間＝「そのモデル専用のものさし」** — 例え: 家具の組み立て説明書。説明書の中の座標は「その家具の中だけで通じる住所」。部屋のどこに置くかは説明書には書いていない。
3. **ワールド空間＝「世界共通のものさし」** — ゲームのステージ全体で共通の原点。例え: 部屋の間取り図。
4. **乗せ替え作業の3点セット: 位置・回転・大きさ** — UnityやUEのインスペクタで毎日触っている「Transform」がまさにこれ。移動(Position)・回転(Rotation)・拡大縮小(Scale)の3つを指定すれば、ローカルの全頂点をワールドの住所に一括変換できる。
5. **「ワールド行列」という言葉だけ覚える** — この3点セットをGPUが一瞬で適用できる形にまとめた「変換レシピカード」を行列（ワールド行列）と呼ぶ。中身の数字は見なくてよい。「レシピカードを頂点全員に配って一斉に引っ越しさせる」イメージ図を載せる。
6. **同じモデルを100本置くとき** — 頂点データは1セットのまま、レシピカード（行列）を100枚用意するだけ。だから同じ木を大量に置いても頂点データは増えない（インスタンシングの入り口。用語は脚注扱い）。
7. **よくある誤解** — 「Scaleを2にするとデータの座標が書き換わる」→ 元データは不変。変換は毎フレーム計算で適用されるだけ。だからいつでも1に戻せる。

デモC（v1の木と岩デモを拡張）:
* v1同様の2画面（プレイヤー視点/神の視点）＋木・岩のTransformスライダー。
* 追加: 「ローカル空間をのぞく」トグル。ONにすると選択モデルだけが原点にある状態の小窓が出て、ワールドへの矢印アニメーションが表示される。
* 追加: 頂点1個をクリックすると「ローカル座標→ワールド座標」の数値変化が吹き出しで表示される。
* ガイド付きツアーあり。

---

### 3.4 ch2b-view.html「ビュー空間 — カメラが世界の中心になる」

**このページで分かること:** カメラにはフィルムがある訳ではなく、世界の方を動かしている／ビュー空間=カメラを原点にした座標への再計算。

本文構成:

1. **問題提起: 「カメラに映る」とはどういう計算か** — 現実のカメラはレンズに光が入る。CGのカメラは何も受け取らない。では何をしている？
2. **発想の転換: カメラを動かすのではなく、世界を逆向きに動かす** — 例え: 電車の窓。自分が前に進むと、景色は後ろに流れる。GPUは毎フレーム「世界の全頂点を、カメラから見た位置に引き寄せ直す」計算をしている。この節がこのページの心臓部。図解を3枚以上使い、カメラが3歩前進する＝全物体が3歩手前に来る、を段階的に見せる。
3. **ビュー空間＝「カメラが原点(0,0,0)のものさし」** — 木の住所が「ワールドで(4, 0, −3)」から「カメラから見て前方9m・右4m」に翻訳される。
4. **これも「レシピカード」1枚（ビュー行列）** — ワールド行列と同じ仕組み。カメラの位置と向きから作られる。
5. **毎フレーム、全頂点に対して行われている** — 60fpsなら1秒に60回、何百万頂点を翻訳し直している。ここで「頂点数が多いと重い」の実感を初めて数字で持たせる（第4章への伏線）。
6. **よくある誤解** — 「カメラを増やすと世界のコピーが増える」→ 増えない。レシピカードが増えるだけ。

デモD（v1のカメラ移動デモを拡張）:
* v1同様の2画面＋カメラ移動（十字キー/ボタン）。
* 追加: 「世界を動かすモード」トグル。ONにすると左画面は固定のまま、右画面（神の視点）で**世界全体がカメラと逆向きに滑る**様子を表示。「カメラが進む」と「世界が引き寄せられる」が同じ結果になることを目で確認できる。本ページの核となるギミック。
* 数値readout（木・岩のビュー空間座標）はv1から継承し、「前方○m・右○m」の日本語表記を併記。
* ガイド付きツアーあり。

---

### 3.5 ch2c-projection.html「投影とクリップ — 映る範囲を決める」

**このページで分かること:** 視錐台=カメラに映る領域の箱／FOV・Near・Farの意味／遠近感（遠くのものが小さい）が作られる場所。

本文構成:

1. **問題提起: カメラは全部を映せない** — 視野の外・後ろ・遠すぎるものは映らない。どこで切っている？
2. **視錐台（しすいだい／Frustum）＝「映る範囲の箱」** — ピラミッドの頭を切った形。例え: 懐中電灯の光の広がり。4つの側面＋Near面＋Far面の6枚の壁。
3. **FOV（画角）** — 箱の開き具合。広角レンズ/望遠レンズの写真の例を添える。広げると多く映るが1つ1つは小さくなる。
4. **Near面とFar面** — なぜ手前も切るのか（カメラにめり込んだ物を描かないため）、なぜ遠くを切るのか（無限遠まで計算できないため）。ゲームで遠景が消える「ドロー距離」の正体。
5. **遠近感はここで生まれる** — 同じ大きさの物でも、箱の奥にあるほど画面上では小さく潰される（透視投影）。例え: 台形の箱を正面から押し潰して直方体にすると、奥の物ほど縮む、の図解アニメーション。
6. **クリップ＝箱の外の頂点を捨てること** — 「クリップ空間」という名前はこの切り捨て（クリッピング）から。捨てるのは描画の前。無駄な計算をしない工夫の第一歩（カリングという言葉をここで初出、第5章への伏線と明言）。
7. **よくある誤解** — 「Farを伸ばしても見た目が変わらなければタダ」→ 映る物が増えれば計算も増える。

デモE（v1の視錐台デモを拡張）:
* v1同様: 2画面、FOV/Near/Farスライダー、箱の外の頂点が赤くなり消える。
* 追加: 「押し潰しアニメーション」ボタン。神の視点で視錐台がグニュッと直方体に正規化され、奥の物体ほど小さくなる様子をスローで見せる（遠近感の発生源の可視化）。
* 追加: 画面内オブジェクト数/クリップされた数のカウンター。
* ガイド付きツアーあり。

---

### 3.6 ch2d-rasterize.html「ラスタライズ — 三角形がピクセルになる」

**このページで分かること:** ディスプレイはマス目でしか光れない／三角形の内側のマス目を洗い出す作業がラスタライズ／ここまでが「頂点の旅」の終点。

本文構成:

1. **問題提起: ディスプレイは线を引けない** — 画面は小さな光る点（ピクセル）の格子でしかない。斜めの線も、実際はマス目の階段。スマホ画面を拡大した写真の例。
2. **スクリーン空間＝「解像度のマス目座標」** — 1920×1080なら横1920マス・縦1080マス。3D座標がついに「何行目・何列目」という2Dの整数に落ちる。
3. **ラスタライズの手順** — ①三角形の3頂点を画面のマス目位置に置く ②上の行から順にスキャン ③各マスの中心が三角形の内側なら「塗る候補」にする。デモFのスロー再生と完全対応させる。
4. **境界のマスはどうする？** — マスの中心が内側かどうかで判定する、という規則だけ紹介（アンチエイリアスは脚注で名前だけ）。
5. **頂点の旅・全行程のふりかえり** — ローカル→ワールド→ビュー→クリップ→スクリーンの5駅を、駅の路線図ふうの図解1枚にまとめる。各駅で「何のための変換だったか」を一行ずつ復習。この図は以降の章でも再掲する。
6. **よくある誤解** — 「ラスタライズで色が塗られる」→ この段階では「どのマスを塗るべきか」の洗い出しまで。何色にするかは次章（パスの話）。

デモF（v1のラスタライズデモを拡張）:
* v1同様のスロー再生グリッド。
* 追加: 解像度切り替え（16×9 / 32×18 / 64×36）。解像度が上がるほど輪郭が滑らかになるが、塗るマスの数（＝仕事量）が増えることをカウンターで見せる。
* 追加: 三角形の頂点をドラッグで動かせる。
* ガイド付きツアーあり。

---

### 3.7 ch3a-gbuffer.html「G-Buffer — 絵の『素材』を先に作る」

**このページで分かること:** 現代ゲームは一発で色を塗らない／深度・法線・材質という「素材レイヤー」を先に保存する／なぜ分けるのか。

本文構成:

1. **問題提起: マス目が決まった。すぐ色を塗ればいい？** — 昔のゲームはそうしていた（フォワードレンダリング。用語は軽く）。しかしライトが増えると破綻する。その理由を先に予告してから本題へ。
2. **料理の例え: 下ごしらえと調理を分ける** — 素材（切った野菜）を全部バットに並べてから、一気に火を通す方が効率的。G-Bufferは「バットに並んだ下ごしらえ済みの素材」。
3. **素材レイヤーその1: 深度（Depth）** — 各マスに「カメラからの距離」を保存。手前の物が奥の物を隠す判定（Zテスト）に使う。白黒画像として可視化できる。
4. **素材レイヤーその2: 法線（Normal）** — 各マスに「表面がどっちを向いているか」を保存。光の当たり方の計算に必須。RGBのサイケデリックな画像になる理由（向きXYZを色RGBに割り当てているだけ）を丁寧に説明。
5. **素材レイヤーその3: ベースカラー/ラフネス/メタリック** — 「地の色」「ツルツル度」「金属度」。アーティストがマテリアルで設定しているパラメータがそのまま保存される。
6. **なぜ分けるのか（本題）** — 色を最終決定する前に素材を保存しておけば、「画面に実際に見えているマスだけ」に対して後から照明計算できる。隠れて見えない部分に高価な計算をしなくて済む。
7. **よくある誤解** — 「G-Bufferは1枚の画像」→ 複数枚の画像セット。デバッグビューで切り替えて見るのはそのうちの1枚。

デモG（v1のパス切り替えデモを分割・拡張）:
* v1のタブ切り替え（Final/Normal/Depth/BaseColor）を本ページに配置。
* 追加: 画面上の任意ピクセルをクリックすると、そのマスに保存されている深度・法線・ベースカラーの値をカード表示（「このマスの素材袋の中身」）。
* ガイド付きツアーあり。

---

### 3.8 ch3b-lighting.html「ライティングパス — 素材に光を当てる」

**このページで分かること:** 照明計算は「素材レイヤーを読みながら画面のマスごとに一斉に」行われる／影はシャドウマップという別素材。

本文構成:

1. **前ページの素材がここで火にかけられる** — 料理の例えの続き。
2. **1マスぶんの照明計算を日本語で読む** — 「このマスの表面は上向き（法線）。太陽は右上にある。だから明るめ。地の色は赤。よって明るい赤」。数式ゼロで、判断の流れだけを箇条書きにする。
3. **これを全マスで一斉に** — 200万マス（フルHD）で同時実行。GPUが得意な「同じ作業の超並列」の初出。GPU=何千人の単純作業アルバイト、CPU=少数の熟練職人の例え。
4. **影（シャドウ）はどう決まる？** — 光源から見た深度画像（シャドウマップ）と照らし合わせ、「光源から見えないマス=影」。ライトから撮った写真と比べる、という説明図。
5. **ライトが増えるとどうなる** — マスごとの判断が増える。ただしG-Buffer方式なら「見えているマスだけ」が対象なので被害が抑えられる、という前ページの回収。
6. **よくある誤解** — 「影は物体にくっついている」→ 影は毎フレーム、光と遮蔽の位置関係から計算し直される。

デモH:
* シーンは前ページと共通。タブに「ライティングのみ」「シャドウマップの中身」を追加。
* 追加: 太陽の方向をドラッグで回せる。法線ビューと最終画面を並べ、太陽を回すと明暗がどう追従するかを見る。
* ガイド付きツアーあり。

---

### 3.9 ch3c-transparent-post.html「半透明とポストプロセス — 1枚の絵を仕上げる」

**このページで分かること:** ガラスや煙が特別扱いされる理由／最後に画面全体へかける化粧（ポストプロセス）／1フレームの全工程整理。

本文構成:

1. **半透明はなぜ後回しか** — G-Buffer方式は「1マスに1つの表面」しか素材を保存できない。ガラス越しの景色は「1マスに2つ以上の表面」なので、素材分離が破綻する。だからライティングが終わった絵の上に、昔ながらの方法で直接ブレンドして重ね書きする。
2. **アルファブレンド＝色の混ぜ算** — 「奥の色70%＋ガラスの色30%」のような混合。描く順序が大事（奥から手前）という制約も、例え（透明フィルムを重ねる順番）で説明。
3. **ポストプロセス＝完成写真への加工** — もう3Dの話ではなく、出来上がった1枚の2D画像への加工。被写界深度・モーションブラー・ブルーム・カラーグレーディングを、スマホの写真編集アプリのフィルタに例えて1つずつ説明。
4. **1フレームの全工程を通しで見る** — 不透明→ライティング→半透明→ポストプロセスの4コマ漫画式の図解。「これが1/60秒ごとに繰り返されている」。
5. **よくある誤解** — 「ポストプロセスは軽い飾り」→ 画面全ピクセルへの処理なので解像度に比例して重い。

デモI:
* 1つのシーンに対し、工程スライダー（0:G-Bufferのみ → 1:ライティング済み → 2:半透明合成済み → 3:ポスト適用済み）を左右に動かすと絵が段階的に完成していく「ビフォーアフター」UI。
* ポストプロセスはブルーム/ビネット/色調の簡易実装をON/OFFできる。
* ガイド付きツアーあり。

---

### 3.10 ch4-quad-overdraw.html「クアッドの罠 — 遠景の高精細モデルはなぜ重い」

**このページで分かること:** 頂点の計算量は見た目の大きさと無関係／GPUは2×2の4マス単位でしか塗れない／マイクロポリゴンが引き起こす無駄の実態。

本文構成は v1 の第Ⅳ章の内容を、以下のように3段階に分けてそれぞれ倍以上に厚くする。

1. **前半: 遠景でも頂点計算はフルに走る** — 数百万ポリゴンの岩が米粒サイズに映るとき、画面上の仕事はほぼゼロなのに、頂点の旅（第2章の5駅）は数百万回まるごと実行されている。「画面がスカスカなのにGPUが忙しい」というねじれを、宅配の例え（荷物1個のために巨大トラックを満載時と同じ手順で運行する）で説明。
2. **中盤: クアッドという最小単位** — GPUが2×2の4マスセットでしか塗れないハードウェア上の決まり。なぜそんな決まりがあるのか（隣のマスとの差分からテクスチャの縮小率を知るため）を、「隣と見比べないと分からない作業」の例えで丁寧に。ヘルパーピクセル（結果を捨てる3マス）の説明。
3. **後半: クアッドオーバードロー** — 微細な三角形が1マスに100枚重なると、4マス一括計算が100回走る。「画面上の1点」の裏で400マス分の計算。ここまでの積み上げ（ラスタライズ→クアッド→密集）を順に踏むので、v1のような駆け足にならないこと。
4. **よくある誤解** — 「ポリゴンを増やしても描く面積が同じなら同じ負荷」→ 大間違い、を数字で。

デモJ（v1のクアッドシミュレーターを拡張）:
* v1同様の4マス拡大キャンバス＋距離スライダー＋負荷メーター＋無駄カウンター。
* 追加: 「1枚ずつ進める」ステップ実行ボタン（三角形1枚ごとの4マス起動を1回ずつ確認できる）。
* 追加: 上部に実際の3Dビュー（岩が遠ざかって米粒になる）を置き、下の4マス拡大図と連動させる。「見た目」と「内部の空回り」の対比を1画面で見せる。
* ガイド付きツアーあり。

---

### 3.11 ch5-meshlet.html「Meshlet — 塊で仕分けるハードウェアの新常識」

**このページで分かること:** モデルを小分けパックに事前分割する発想／タスクシェーダー（現場監督）とメッシュシェーダー（実行部隊）／「計算が始まる前に捨てる」ことの意味。

本文構成（v1の第Ⅴ章を4節に分けて厚くする）:

1. **問題の整理と発想の転換** — 第4章の無駄は「1頂点ずつ律儀に処理する行列（ぎょうれつ）方式」が原因。荷物を1個ずつ検品するのをやめ、「箱単位で要る/要らないを判断してから開ける」方式へ。
2. **Meshletの構造** — 最大64頂点・126三角形の小分けパック。なぜその大きさか（GPUの手元の作業机=高速メモリにちょうど乗るサイズ）。倉庫のコンテナの例え。
3. **タスクシェーダー＝現場監督** — パックごとに「カメラの後ろ？」「裏向き？」「遠すぎて1ピクセル未満？」を一括判定し、ダメなパックは開封すらしない（カリング）。第2章cで出た「カリング」の回収。
4. **メッシュシェーダー＝実行部隊** — 生き残ったパックだけを並列処理。従来の頂点シェーダーとの違いは「担当単位が頂点1個からパック1箱になった」ことだけ、と単純化して締める。
5. **よくある誤解** — 「Meshletはソフトの機能」→ グラボ側の（ハードウェアの）新規格。UEでもUnityでも自作エンジンでも使える土台の話。

デモK（v1のメッシュレットビューワーを拡張）:
* v1同様: パッチワーク色分け表示、従来/Meshletトグル、裏側パックの消滅、距離カリング、負荷メーター。
* 追加: 「監督の判断ログ」パネル。カリングが起きるたび「パック#12: 裏向きのため破棄」等の1行ログが流れる（間引いて表示）。判断が「描画前」に行われていることを実況で体感させる。
* ガイド付きツアーあり。

---

### 3.12 ch6-nanite.html「Nanite — エンジンによる全自動化と裏ルート」

**このページで分かること:** NaniteはMeshletの上に築かれたUE5のソフトウェアシステム／必要な解像度のデータだけ読む／小さすぎる三角形はGPU標準の塗り方を捨てて自前で塗る。

本文構成（v1の第Ⅵ章を4節に分けて厚くする）:

1. **MeshletとNaniteの関係を最初に整理** — ハードの新常識(第5章)を道具として使いつつ、その上に独自の仕組みを重ねたのがNanite。「高速道路（ハード）と、その上を走る自動運転トラック網（ソフト）」の例え。
2. **クラスターのピラミッド（DAG階層）とストリーミング** — 128三角形のクラスターを、高精細から超低精細まで段階的に束ねたピラミッド構造でSSDに保存。プレイ中は「今の距離で必要な段」だけをVRAMに読む。地図アプリのズーム（近づくと詳細タイルだけ読み込む）の例えを軸に説明。
3. **継続的LOD** — 従来LODの「パッと切り替わる」不自然さ（ポップイン）が、クラスター単位の細かい差し替えで消える。常に「1ピクセル=1ポリゴン」付近を維持するという目標値の意味。
4. **ソフトウェア・ラスタライザー＝裏ルート** — 第4章のクアッドの罠は「GPU標準の塗り方」に固有の罠。Naniteは三角形が数ピクセル未満になったら標準ルートを通さず、汎用計算ユニットで1マスずつ直接塗る。「4人1組ルールの食堂が混んだら、自前の弁当を配る」の例え。罠を回避する場所が「ルールの中での工夫」ではなく「ルールの外」であることを強調。
5. **よくある誤解** — 「Naniteを使えば何でも無限に置ける」→ 半透明や変形するものが苦手など制約はある（詳細は割愛と明記）。

デモL（v1のハイブリッドシミュレーターを拡張）:
* v1同様: 距離による網目の自動増減（連続LODの視覚化）、HW=青/SW=緑の色分けモード、負荷メーターのフラット維持。
* 追加: 「従来LOD比較」トグル。従来の3段階LODモデルと並べて表示し、従来側だけ距離の閾値でパッと切り替わる（ポップイン）様子を対比。
* 追加: ストリーミング可視化ミニパネル: 距離に応じて「SSD→VRAMに載っている段」がハイライトされるピラミッド図。
* ガイド付きツアーあり。

---

### 3.13 ch7-summary.html「まとめ — アーティストの未来」

1. **13ページの旅の全体復習** — 「座標→頂点の5駅→素材と調理→クアッドの罠→塊仕分け→全自動化」を1枚の大きな路線図にまとめる（各章ページへのリンク埋め込み）。
2. **ワークフローの変化 before/after** — v1の総括の内容を継承しつつ、各項目に「なぜそうなるのか＝どの章で学んだか」の参照を付ける。
3. **卒業クイズ** — 全章から10問。合格すると称号表示などの軽い演出。
4. **さらに学びたい人へ** — 次のステップの案内（シェーダー入門、UE/Unityの実機デバッグビューの見方など、外部リンクなしの一般的な指針で良い）。

---

### 3.14 index.html（表紙）

* サイトの目的・対象読者・「数式ゼロ」の宣言。
* 学習マップ: 13ページのカード一覧（各カードに所要時間目安と一言サマリー）。
* 「最初から読む」ボタン → ch1へ。

### 3.15 glossary.html（用語集）

* 全ページの用語を五十音順に集約。各項目は「一言定義（30字以内）＋詳しい説明（2〜4文）＋初出ページへのリンク」。
* 各章ページの用語欄からアンカーリンクで飛べる。

---

## 4. 実装方針（v1から継承・追加）

* 静的HTML/CSS/JS。Three.jsはローカル同梱（`js/vendor/`）を継続。
* v1で実装済みのデモ（A〜Lの元になるもの）は流用し、各ページへ分割配置＋本仕様の「追加」項目を実装する。
* 共通コンポーネント: ヘッダーナビ、前へ/次へフッター、クイズウィジェット、ガイド付きツアー（吹き出し＋自動操作）、チェックリスト、用語ツールチップ。
* 文章量の目安: 各章ページ本文3,000〜6,000字。見出しは h3 以下を細かく切り、1段落は3〜4文まで。
* 図解はSVGまたはCSSで作図（外部画像に依存しない）。
* GitHub Pages（プロジェクトページ）で公開。相対パスで動作すること。
